# x86_64 only to conserve the free-tier CI quota; add the aarch64-linux
# patterns back if the repo moves to a plan with more capacity.
builds:
  include:
    - "checks.x86_64-linux.*"
    - "packages.x86_64-linux.*"
    - "devShells.x86_64-linux.*"
